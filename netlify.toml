[build]
  command = "npm run build"
  publish = "dist"

[functions]
  # If you add Netlify Functions later, set the folder here (optional)
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
